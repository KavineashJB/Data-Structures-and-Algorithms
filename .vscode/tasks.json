{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "compile and run java",
            "type": "shell",
            "command": "javac ${file}; Get-Content .vscode/input.txt | java -cp ${fileDirname} ${fileBasenameNoExtension} | Set-Content .vscode/output.txt",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
